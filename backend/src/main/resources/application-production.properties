spring.application.name=backend

# Production Database Configuration
spring.datasource.url=${DATABASE_URL:postgresql://postgres:OeAwgjDinwCLicGMCmAMGrSpFRLRszrY@shinkansen.proxy.rlwy.net:24200/railway}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration for Production
spring.web.cors.allowed-origins=${FRONTEND_URL:https://your-frontend-url.onrender.com}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Server Configuration
server.port=${PORT:8080}

# Logging
logging.level.com.backend=INFO
logging.level.org.springframework.web.cors=DEBUG